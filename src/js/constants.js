/**
 * List of components to be loaded depending on the page.
 */
export const COMPONENTS_TO_LOAD = {
	MAIN: 'main',
};

/**
 * The delay between looking for a button in {@link https://developer.mozilla.org/pl/docs/Web/API/WindowOrWorkerGlobalScope/setInterval milliseconds}.
 */
export const SKIPPING_BUILDING_INTERVAL = 1000;
